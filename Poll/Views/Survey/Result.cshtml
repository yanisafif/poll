@model IEnumerable<Poll.Services.ViewModel.ResultViewModel>

@{
    ViewData["Title"] = "Result";
}

<h1>Result</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdChoice)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NameChoice)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.vote)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdChoice)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NameChoice)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.vote)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
